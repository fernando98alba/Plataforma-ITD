<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <%if empresa.areas.length == 0%>
          <h1 class="modal-title fs-5" id="exampleModalLabel">Elija los participantes de la medición</h1>
        <%else%>
          <h1 class="modal-title fs-5" id="exampleModalLabel">Elija las áreas que participaran de la medición</h1>
        <%end%>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%if empresa.areas.length == 0%>
          <%= render partial: "form", locals: {empresa: empresa, itdcon: itdcon, participants: participants}%>
        <%else%>
          <%= render partial: "form_area", locals: {empresa: empresa, itdcon: itdcon, areas: empresa.areas}%>
        <%end%>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
        <button type="submit" class="btn btn-primary" name="submit" value="Submit" data-bs-dismiss="modal" form="itdcon_form">Crear medición</button>
      </div>
    </div>
  </div>
</div>


